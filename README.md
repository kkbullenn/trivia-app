# trivia-app

⚠️ IMPORTANT: READ THE FILE README_DB.md IN THE /db FOLDER FOR THE FULL DATABASE CONNECTION GUIDE AND TEAM INSTRUCTIONS.

NOTE: On branch `db-team-DAO` the runtime outputs (WEB-INF/classes and WEB-INF/lib) were moved to `backup/` to provide a clean source layout for DAO work. See `README_CLEANUP.md` for details.